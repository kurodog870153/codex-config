interface:
  display_name: "Task"
  short_description: "Load layered hierarchical task-planning rules"
  default_prompt: "Use $task general web backend java to load a hierarchy of task-planning rules for this request."

policy:
  allow_implicit_invocation: true
